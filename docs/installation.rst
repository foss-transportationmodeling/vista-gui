.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install vista-gui

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv vista-gui
    $ pip install vista-gui
